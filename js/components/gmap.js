/*'use strict';

*/
/*
GMaps.geocode({
  address: $('#address').val(),
  callback: function(results, status) {
    if (status == 'OK') {
      var latlng = results[0].geometry.location;
      map.setCenter(latlng.lat(), latlng.lng());
      map.addMarker({
        lat: latlng.lat(),
        lng: latlng.lng()
      });
    }
  }
});
*/

/*const Geolocation = (stations,update) => {
	const container = $('<div></div>');
	const map = $('<div id="map"></div>');
	const stationName = $('<p>'+stations.name+'</p>');
	const distance = $('<p></p>');
	const g86 = $('<div>86</div>');
	const g90 = $('<div>86</div>');
	const g95 = $('<div>86</div>');
	const g97 = $('<div>86</div>');


}*/